// Include template method definitions (needed for explicit instantiation)
#include "all.cpp"

// Note: all.cpp already includes explicit_instantiations.h at the end,
// which provides the JitProvider explicit instantiations.
// This file exists as a standalone compilation unit alternative.
