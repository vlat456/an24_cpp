// Auto-generated by codegen from library/*.blueprint
// DO NOT EDIT - changes will be overwritten

#pragma once

#include <cstdint>
#include <string>
#include <unordered_map>
#include <optional>
#include <stdexcept>
#include <vector>
#include <variant>

#include "provider.h"
#include "all.h"
#include "port_names.h"
#include "core/model/component_kind.h"

constexpr size_t AND_PORT_COUNT = 3;
constexpr size_t AZS_PORT_COUNT = 6;
constexpr size_t Accumulator_PORT_COUNT = 2;
constexpr size_t Add_PORT_COUNT = 3;
constexpr size_t Any_V_to_Bool_PORT_COUNT = 2;
constexpr size_t AsymSlewRate_PORT_COUNT = 2;
constexpr size_t AsymTMO_PORT_COUNT = 2;
constexpr size_t Bus_PORT_COUNT = 1;
constexpr size_t Clamp_PORT_COUNT = 4;
constexpr size_t Comparator_PORT_COUNT = 3;
constexpr size_t ControlledCurrentSource_PORT_COUNT = 3;
constexpr size_t ControlledVoltageSource_PORT_COUNT = 8;
constexpr size_t CurrentSense_PORT_COUNT = 3;
constexpr size_t Divide_PORT_COUNT = 3;
constexpr size_t ElectricHeater_PORT_COUNT = 2;
constexpr size_t ElectricPump_PORT_COUNT = 3;
constexpr size_t ElectricalConductance_PORT_COUNT = 2;
constexpr size_t ElectricalSource_PORT_COUNT = 2;
constexpr size_t FastTMO_PORT_COUNT = 2;
constexpr size_t FuelTank_PORT_COUNT = 2;
constexpr size_t Generator_PORT_COUNT = 2;
constexpr size_t GidroAccumulator_PORT_COUNT = 2;
constexpr size_t Greater_PORT_COUNT = 3;
constexpr size_t GreaterEq_PORT_COUNT = 3;
constexpr size_t Gyroscope_PORT_COUNT = 1;
constexpr size_t HoldButton_PORT_COUNT = 4;
constexpr size_t IndicatorLight_PORT_COUNT = 3;
constexpr size_t InertiaNode_PORT_COUNT = 5;
constexpr size_t Integrator_PORT_COUNT = 4;
constexpr size_t Inverter_PORT_COUNT = 2;
constexpr size_t KnobSwitch_PORT_COUNT = 8;
constexpr size_t LUT_PORT_COUNT = 2;
constexpr size_t LerpNode_PORT_COUNT = 2;
constexpr size_t Lesser_PORT_COUNT = 3;
constexpr size_t LesserEq_PORT_COUNT = 3;
constexpr size_t Max_PORT_COUNT = 3;
constexpr size_t Merger_PORT_COUNT = 3;
constexpr size_t Min_PORT_COUNT = 3;
constexpr size_t Monostable_PORT_COUNT = 2;
constexpr size_t Multiply_PORT_COUNT = 3;
constexpr size_t NAND_PORT_COUNT = 3;
constexpr size_t NOT_PORT_COUNT = 2;
constexpr size_t Normalize_PORT_COUNT = 4;
constexpr size_t OR_PORT_COUNT = 3;
constexpr size_t P_PORT_COUNT = 3;
constexpr size_t PD_PORT_COUNT = 3;
constexpr size_t PI_PORT_COUNT = 7;
constexpr size_t PID_PORT_COUNT = 3;
constexpr size_t Positive_V_to_Bool_PORT_COUNT = 2;
constexpr size_t Radiator_PORT_COUNT = 2;
constexpr size_t RefNode_PORT_COUNT = 1;
constexpr size_t Relay_PORT_COUNT = 5;
constexpr size_t Resistor_PORT_COUNT = 2;
constexpr size_t RotarySwitch1ToN_PORT_COUNT = 8;
constexpr size_t RotarySwitchNTo1_PORT_COUNT = 8;
constexpr size_t SampleHold_PORT_COUNT = 3;
constexpr size_t SlewRate_PORT_COUNT = 2;
constexpr size_t Slider_PORT_COUNT = 2;
constexpr size_t SolenoidValve_PORT_COUNT = 3;
constexpr size_t Splitter_PORT_COUNT = 3;
constexpr size_t Spring_PORT_COUNT = 3;
constexpr size_t Subtract_PORT_COUNT = 3;
constexpr size_t Switch_PORT_COUNT = 4;
constexpr size_t TempSensor_PORT_COUNT = 2;
constexpr size_t TimeDelay_PORT_COUNT = 2;
constexpr size_t Transformer_PORT_COUNT = 2;
constexpr size_t Value_PORT_COUNT = 1;
constexpr size_t VariableConductance_PORT_COUNT = 5;
constexpr size_t VoltageSense_PORT_COUNT = 5;
constexpr size_t Voltmeter_PORT_COUNT = 1;
constexpr size_t XOR_PORT_COUNT = 3;

constexpr const char* AND_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* AZS_PORTS[] = {
    "control",
    "state",
    "temp",
    "tripped",
    "v_in",
    "v_out"
};
constexpr const char* Accumulator_PORTS[] = {
    "in",
    "out"
};
constexpr const char* Add_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* Any_V_to_Bool_PORTS[] = {
    "Vin",
    "o"
};
constexpr const char* AsymSlewRate_PORTS[] = {
    "in",
    "out"
};
constexpr const char* AsymTMO_PORTS[] = {
    "in",
    "out"
};
constexpr const char* Bus_PORTS[] = {
    "v"
};
constexpr const char* Clamp_PORTS[] = {
    "in",
    "max",
    "min",
    "out"
};
constexpr const char* Comparator_PORTS[] = {
    "Va",
    "Vb",
    "o"
};
constexpr const char* ControlledCurrentSource_PORTS[] = {
    "cmd",
    "v_neg",
    "v_pos"
};
constexpr const char* ControlledVoltageSource_PORTS[] = {
    "cmd",
    "gain",
    "i_out",
    "max_v",
    "min_v",
    "offset",
    "v_neg",
    "v_pos"
};
constexpr const char* CurrentSense_PORTS[] = {
    "i_out",
    "v_in",
    "v_out"
};
constexpr const char* Divide_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* ElectricHeater_PORTS[] = {
    "heat_out",
    "power"
};
constexpr const char* ElectricPump_PORTS[] = {
    "p_in",
    "p_out",
    "v_in"
};
constexpr const char* ElectricalConductance_PORTS[] = {
    "v_in",
    "v_out"
};
constexpr const char* ElectricalSource_PORTS[] = {
    "v_in",
    "v_out"
};
constexpr const char* FastTMO_PORTS[] = {
    "in",
    "out"
};
constexpr const char* FuelTank_PORTS[] = {
    "flow_out",
    "level_out"
};
constexpr const char* Generator_PORTS[] = {
    "v_in",
    "v_out"
};
constexpr const char* GidroAccumulator_PORTS[] = {
    "p_in",
    "p_out"
};
constexpr const char* Greater_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* GreaterEq_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* Gyroscope_PORTS[] = {
    "input"
};
constexpr const char* HoldButton_PORTS[] = {
    "control",
    "state",
    "v_in",
    "v_out"
};
constexpr const char* IndicatorLight_PORTS[] = {
    "brightness",
    "v_in",
    "v_out"
};
constexpr const char* InertiaNode_PORTS[] = {
    "damping",
    "inv_inertia",
    "mass",
    "rpm_out",
    "torque_in"
};
constexpr const char* Integrator_PORTS[] = {
    "gain",
    "in",
    "out",
    "reset"
};
constexpr const char* Inverter_PORTS[] = {
    "ac_out",
    "dc_in"
};
constexpr const char* KnobSwitch_PORTS[] = {
    "control",
    "position",
    "throw1",
    "throw2",
    "throw3",
    "throw4",
    "throw5",
    "wiper"
};
constexpr const char* LUT_PORTS[] = {
    "input",
    "output"
};
constexpr const char* LerpNode_PORTS[] = {
    "input",
    "output"
};
constexpr const char* Lesser_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* LesserEq_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* Max_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* Merger_PORTS[] = {
    "i1",
    "i2",
    "o"
};
constexpr const char* Min_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* Monostable_PORTS[] = {
    "in",
    "out"
};
constexpr const char* Multiply_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* NAND_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* NOT_PORTS[] = {
    "A",
    "o"
};
constexpr const char* Normalize_PORTS[] = {
    "in",
    "max",
    "min",
    "out"
};
constexpr const char* OR_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* P_PORTS[] = {
    "feedback",
    "output",
    "setpoint"
};
constexpr const char* PD_PORTS[] = {
    "feedback",
    "output",
    "setpoint"
};
constexpr const char* PI_PORTS[] = {
    "Ki",
    "Kp",
    "feedback",
    "output",
    "output_max",
    "output_min",
    "setpoint"
};
constexpr const char* PID_PORTS[] = {
    "feedback",
    "output",
    "setpoint"
};
constexpr const char* Positive_V_to_Bool_PORTS[] = {
    "Vin",
    "o"
};
constexpr const char* Radiator_PORTS[] = {
    "heat_in",
    "heat_out"
};
constexpr const char* RefNode_PORTS[] = {
    "v"
};
constexpr const char* Relay_PORTS[] = {
    "control",
    "hold_threshold",
    "state",
    "v_in",
    "v_out"
};
constexpr const char* Resistor_PORTS[] = {
    "v_in",
    "v_out"
};
constexpr const char* RotarySwitch1ToN_PORTS[] = {
    "control",
    "position",
    "throw1",
    "throw2",
    "throw3",
    "throw4",
    "throw5",
    "wiper"
};
constexpr const char* RotarySwitchNTo1_PORTS[] = {
    "control",
    "position",
    "throw1",
    "throw2",
    "throw3",
    "throw4",
    "throw5",
    "wiper"
};
constexpr const char* SampleHold_PORTS[] = {
    "in",
    "out",
    "trigger"
};
constexpr const char* SlewRate_PORTS[] = {
    "in",
    "out"
};
constexpr const char* Slider_PORTS[] = {
    "control",
    "out"
};
constexpr const char* SolenoidValve_PORTS[] = {
    "ctrl",
    "flow_in",
    "flow_out"
};
constexpr const char* Splitter_PORTS[] = {
    "i",
    "o1",
    "o2"
};
constexpr const char* Spring_PORTS[] = {
    "force_out",
    "pos_a",
    "pos_b"
};
constexpr const char* Subtract_PORTS[] = {
    "A",
    "B",
    "o"
};
constexpr const char* Switch_PORTS[] = {
    "control",
    "state",
    "v_in",
    "v_out"
};
constexpr const char* TempSensor_PORTS[] = {
    "temp_in",
    "temp_out"
};
constexpr const char* TimeDelay_PORTS[] = {
    "in",
    "out"
};
constexpr const char* Transformer_PORTS[] = {
    "primary",
    "secondary"
};
constexpr const char* Value_PORTS[] = {
    "o"
};
constexpr const char* VariableConductance_PORTS[] = {
    "cmd",
    "g_max",
    "g_min",
    "v_in",
    "v_out"
};
constexpr const char* VoltageSense_PORTS[] = {
    "gain",
    "offset",
    "out",
    "v_in",
    "v_ref"
};
constexpr const char* Voltmeter_PORTS[] = {
    "v_in"
};
constexpr const char* XOR_PORTS[] = {
    "A",
    "B",
    "o"
};

#include "../../../../blueprint_v2/interface/direction.h"

constexpr bp2::Direction AND_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t AND_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool AND_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction AZS_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t AZS_PORT_DOMAINS[] = {
    16,
    2,
    16,
    2,
    1,
    1
};
constexpr bool AZS_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction Accumulator_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Accumulator_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool Accumulator_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Add_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Add_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Add_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Any_V_to_Bool_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Any_V_to_Bool_PORT_DOMAINS[] = {
    1,
    2
};
constexpr bool Any_V_to_Bool_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction AsymSlewRate_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t AsymSlewRate_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool AsymSlewRate_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction AsymTMO_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t AsymTMO_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool AsymTMO_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Bus_PORT_DIRECTIONS[] = {
    bp2::Direction::InOut
};
constexpr uint8_t Bus_PORT_DOMAINS[] = {
    1
};
constexpr bool Bus_PORT_SOURCE_WRITER[] = {
    false
};
constexpr bp2::Direction Clamp_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Clamp_PORT_DOMAINS[] = {
    2,
    2,
    2,
    2
};
constexpr bool Clamp_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false
};
constexpr bp2::Direction Comparator_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Comparator_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Comparator_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction ControlledCurrentSource_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Output
};
constexpr uint8_t ControlledCurrentSource_PORT_DOMAINS[] = {
    2,
    1,
    1
};
constexpr bool ControlledCurrentSource_PORT_SOURCE_WRITER[] = {
    false,
    false,
    true
};
constexpr bp2::Direction ControlledVoltageSource_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t ControlledVoltageSource_PORT_DOMAINS[] = {
    2,
    2,
    1,
    2,
    2,
    2,
    1,
    1
};
constexpr bool ControlledVoltageSource_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true
};
constexpr bp2::Direction CurrentSense_PORT_DIRECTIONS[] = {
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t CurrentSense_PORT_DOMAINS[] = {
    1,
    1,
    1
};
constexpr bool CurrentSense_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Divide_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Divide_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Divide_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction ElectricHeater_PORT_DIRECTIONS[] = {
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t ElectricHeater_PORT_DOMAINS[] = {
    16,
    1
};
constexpr bool ElectricHeater_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction ElectricPump_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t ElectricPump_PORT_DOMAINS[] = {
    8,
    8,
    1
};
constexpr bool ElectricPump_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction ElectricalConductance_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t ElectricalConductance_PORT_DOMAINS[] = {
    1,
    1
};
constexpr bool ElectricalConductance_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction ElectricalSource_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t ElectricalSource_PORT_DOMAINS[] = {
    1,
    1
};
constexpr bool ElectricalSource_PORT_SOURCE_WRITER[] = {
    false,
    true
};
constexpr bp2::Direction FastTMO_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t FastTMO_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool FastTMO_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction FuelTank_PORT_DIRECTIONS[] = {
    bp2::Direction::Output,
    bp2::Direction::Output
};
constexpr uint8_t FuelTank_PORT_DOMAINS[] = {
    8,
    2
};
constexpr bool FuelTank_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Generator_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Generator_PORT_DOMAINS[] = {
    1,
    1
};
constexpr bool Generator_PORT_SOURCE_WRITER[] = {
    false,
    true
};
constexpr bp2::Direction GidroAccumulator_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t GidroAccumulator_PORT_DOMAINS[] = {
    8,
    8
};
constexpr bool GidroAccumulator_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Greater_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Greater_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Greater_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction GreaterEq_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t GreaterEq_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool GreaterEq_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Gyroscope_PORT_DIRECTIONS[] = {
    bp2::Direction::Input
};
constexpr uint8_t Gyroscope_PORT_DOMAINS[] = {
    1
};
constexpr bool Gyroscope_PORT_SOURCE_WRITER[] = {
    false
};
constexpr bp2::Direction HoldButton_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t HoldButton_PORT_DOMAINS[] = {
    16,
    2,
    1,
    1
};
constexpr bool HoldButton_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false
};
constexpr bp2::Direction IndicatorLight_PORT_DIRECTIONS[] = {
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t IndicatorLight_PORT_DOMAINS[] = {
    1,
    1,
    1
};
constexpr bool IndicatorLight_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction InertiaNode_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t InertiaNode_PORT_DOMAINS[] = {
    2,
    2,
    2,
    4,
    2
};
constexpr bool InertiaNode_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction Integrator_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t Integrator_PORT_DOMAINS[] = {
    2,
    2,
    2,
    2
};
constexpr bool Integrator_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false
};
constexpr bp2::Direction Inverter_PORT_DIRECTIONS[] = {
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t Inverter_PORT_DOMAINS[] = {
    16,
    1
};
constexpr bool Inverter_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction KnobSwitch_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::InOut,
    bp2::Direction::InOut,
    bp2::Direction::InOut,
    bp2::Direction::InOut,
    bp2::Direction::InOut,
    bp2::Direction::InOut
};
constexpr uint8_t KnobSwitch_PORT_DOMAINS[] = {
    2,
    2,
    1,
    1,
    1,
    1,
    1,
    1
};
constexpr bool KnobSwitch_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction LUT_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t LUT_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool LUT_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction LerpNode_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t LerpNode_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool LerpNode_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Lesser_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Lesser_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Lesser_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction LesserEq_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t LesserEq_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool LesserEq_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Max_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Max_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Max_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Merger_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Merger_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Merger_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Min_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Min_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Min_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Monostable_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Monostable_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool Monostable_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Multiply_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Multiply_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Multiply_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction NAND_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t NAND_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool NAND_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction NOT_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t NOT_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool NOT_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Normalize_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Normalize_PORT_DOMAINS[] = {
    2,
    2,
    2,
    2
};
constexpr bool Normalize_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false
};
constexpr bp2::Direction OR_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t OR_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool OR_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction P_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t P_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool P_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction PD_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t PD_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool PD_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction PI_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input
};
constexpr uint8_t PI_PORT_DOMAINS[] = {
    2,
    2,
    2,
    2,
    2,
    2,
    2
};
constexpr bool PI_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction PID_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t PID_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool PID_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Positive_V_to_Bool_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Positive_V_to_Bool_PORT_DOMAINS[] = {
    1,
    2
};
constexpr bool Positive_V_to_Bool_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Radiator_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Radiator_PORT_DOMAINS[] = {
    1,
    16
};
constexpr bool Radiator_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction RefNode_PORT_DIRECTIONS[] = {
    bp2::Direction::Output
};
constexpr uint8_t RefNode_PORT_DOMAINS[] = {
    1
};
constexpr bool RefNode_PORT_SOURCE_WRITER[] = {
    false
};
constexpr bp2::Direction Relay_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Relay_PORT_DOMAINS[] = {
    2,
    2,
    2,
    1,
    1
};
constexpr bool Relay_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction Resistor_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Resistor_PORT_DOMAINS[] = {
    1,
    1
};
constexpr bool Resistor_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction RotarySwitch1ToN_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t RotarySwitch1ToN_PORT_DOMAINS[] = {
    2,
    2,
    1,
    1,
    1,
    1,
    1,
    1
};
constexpr bool RotarySwitch1ToN_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction RotarySwitchNTo1_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t RotarySwitchNTo1_PORT_DOMAINS[] = {
    2,
    2,
    1,
    1,
    1,
    1,
    1,
    1
};
constexpr bool RotarySwitchNTo1_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction SampleHold_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input
};
constexpr uint8_t SampleHold_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool SampleHold_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction SlewRate_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t SlewRate_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool SlewRate_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Slider_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Slider_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool Slider_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction SolenoidValve_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t SolenoidValve_PORT_DOMAINS[] = {
    1,
    8,
    8
};
constexpr bool SolenoidValve_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Splitter_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Output
};
constexpr uint8_t Splitter_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Splitter_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Spring_PORT_DIRECTIONS[] = {
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Input
};
constexpr uint8_t Spring_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Spring_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Subtract_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Subtract_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool Subtract_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
constexpr bp2::Direction Switch_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Switch_PORT_DOMAINS[] = {
    16,
    2,
    1,
    1
};
constexpr bool Switch_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false
};
constexpr bp2::Direction TempSensor_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t TempSensor_PORT_DOMAINS[] = {
    1,
    16
};
constexpr bool TempSensor_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction TimeDelay_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t TimeDelay_PORT_DOMAINS[] = {
    2,
    2
};
constexpr bool TimeDelay_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Transformer_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t Transformer_PORT_DOMAINS[] = {
    1,
    2
};
constexpr bool Transformer_PORT_SOURCE_WRITER[] = {
    false,
    false
};
constexpr bp2::Direction Value_PORT_DIRECTIONS[] = {
    bp2::Direction::Output
};
constexpr uint8_t Value_PORT_DOMAINS[] = {
    2
};
constexpr bool Value_PORT_SOURCE_WRITER[] = {
    false
};
constexpr bp2::Direction VariableConductance_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t VariableConductance_PORT_DOMAINS[] = {
    2,
    2,
    2,
    1,
    1
};
constexpr bool VariableConductance_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction VoltageSense_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output,
    bp2::Direction::Input,
    bp2::Direction::Input
};
constexpr uint8_t VoltageSense_PORT_DOMAINS[] = {
    2,
    2,
    2,
    1,
    1
};
constexpr bool VoltageSense_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false,
    false,
    false
};
constexpr bp2::Direction Voltmeter_PORT_DIRECTIONS[] = {
    bp2::Direction::Input
};
constexpr uint8_t Voltmeter_PORT_DOMAINS[] = {
    1
};
constexpr bool Voltmeter_PORT_SOURCE_WRITER[] = {
    false
};
constexpr bp2::Direction XOR_PORT_DIRECTIONS[] = {
    bp2::Direction::Input,
    bp2::Direction::Input,
    bp2::Direction::Output
};
constexpr uint8_t XOR_PORT_DOMAINS[] = {
    2,
    2,
    2
};
constexpr bool XOR_PORT_SOURCE_WRITER[] = {
    false,
    false,
    false
};
inline std::optional<PortNames> string_to_port_name(const std::string& name) {
    static const std::unordered_map<std::string, PortNames> map = {
        {"A", PortNames::A},
        {"B", PortNames::B},
        {"Ki", PortNames::Ki},
        {"Kp", PortNames::Kp},
        {"Va", PortNames::Va},
        {"Vb", PortNames::Vb},
        {"Vin", PortNames::Vin},
        {"ac_out", PortNames::ac_out},
        {"brightness", PortNames::brightness},
        {"cmd", PortNames::cmd},
        {"control", PortNames::control},
        {"ctrl", PortNames::ctrl},
        {"damping", PortNames::damping},
        {"dc_in", PortNames::dc_in},
        {"feedback", PortNames::feedback},
        {"flow_in", PortNames::flow_in},
        {"flow_out", PortNames::flow_out},
        {"force_out", PortNames::force_out},
        {"g_max", PortNames::g_max},
        {"g_min", PortNames::g_min},
        {"gain", PortNames::gain},
        {"heat_in", PortNames::heat_in},
        {"heat_out", PortNames::heat_out},
        {"hold_threshold", PortNames::hold_threshold},
        {"i", PortNames::i},
        {"i1", PortNames::i1},
        {"i2", PortNames::i2},
        {"i_out", PortNames::i_out},
        {"in", PortNames::in},
        {"input", PortNames::input},
        {"inv_inertia", PortNames::inv_inertia},
        {"level_out", PortNames::level_out},
        {"mass", PortNames::mass},
        {"max", PortNames::max},
        {"max_v", PortNames::max_v},
        {"min", PortNames::min},
        {"min_v", PortNames::min_v},
        {"o", PortNames::o},
        {"o1", PortNames::o1},
        {"o2", PortNames::o2},
        {"offset", PortNames::offset},
        {"out", PortNames::out},
        {"output", PortNames::output},
        {"output_max", PortNames::output_max},
        {"output_min", PortNames::output_min},
        {"p_in", PortNames::p_in},
        {"p_out", PortNames::p_out},
        {"pos_a", PortNames::pos_a},
        {"pos_b", PortNames::pos_b},
        {"position", PortNames::position},
        {"power", PortNames::power},
        {"primary", PortNames::primary},
        {"reset", PortNames::reset},
        {"rpm_out", PortNames::rpm_out},
        {"secondary", PortNames::secondary},
        {"setpoint", PortNames::setpoint},
        {"state", PortNames::state},
        {"temp", PortNames::temp},
        {"temp_in", PortNames::temp_in},
        {"temp_out", PortNames::temp_out},
        {"throw1", PortNames::throw1},
        {"throw2", PortNames::throw2},
        {"throw3", PortNames::throw3},
        {"throw4", PortNames::throw4},
        {"throw5", PortNames::throw5},
        {"torque_in", PortNames::torque_in},
        {"trigger", PortNames::trigger},
        {"tripped", PortNames::tripped},
        {"v", PortNames::v},
        {"v_in", PortNames::v_in},
        {"v_neg", PortNames::v_neg},
        {"v_out", PortNames::v_out},
        {"v_pos", PortNames::v_pos},
        {"v_ref", PortNames::v_ref},
        {"wiper", PortNames::wiper},
    };
    auto it = map.find(name);
    if (it != map.end()) return it->second;
    return std::nullopt;
}

inline std::vector<std::string> get_component_ports(ComponentKind kind) {
    switch (kind) {
        case ComponentKind::AND: return {"A", "B", "o"};
        case ComponentKind::AZS: return {"control", "state", "temp", "tripped", "v_in", "v_out"};
        case ComponentKind::Accumulator: return {"in", "out"};
        case ComponentKind::Add: return {"A", "B", "o"};
        case ComponentKind::Any_V_to_Bool: return {"Vin", "o"};
        case ComponentKind::AsymSlewRate: return {"in", "out"};
        case ComponentKind::AsymTMO: return {"in", "out"};
        case ComponentKind::Bus: return {"v"};
        case ComponentKind::Clamp: return {"in", "max", "min", "out"};
        case ComponentKind::Comparator: return {"Va", "Vb", "o"};
        case ComponentKind::ControlledCurrentSource: return {"cmd", "v_neg", "v_pos"};
        case ComponentKind::ControlledVoltageSource: return {"cmd", "gain", "i_out", "max_v", "min_v", "offset", "v_neg", "v_pos"};
        case ComponentKind::CurrentSense: return {"i_out", "v_in", "v_out"};
        case ComponentKind::Divide: return {"A", "B", "o"};
        case ComponentKind::ElectricHeater: return {"heat_out", "power"};
        case ComponentKind::ElectricPump: return {"p_in", "p_out", "v_in"};
        case ComponentKind::ElectricalConductance: return {"v_in", "v_out"};
        case ComponentKind::ElectricalSource: return {"v_in", "v_out"};
        case ComponentKind::FastTMO: return {"in", "out"};
        case ComponentKind::FuelTank: return {"flow_out", "level_out"};
        case ComponentKind::Generator: return {"v_in", "v_out"};
        case ComponentKind::GidroAccumulator: return {"p_in", "p_out"};
        case ComponentKind::Greater: return {"A", "B", "o"};
        case ComponentKind::GreaterEq: return {"A", "B", "o"};
        case ComponentKind::Gyroscope: return {"input"};
        case ComponentKind::HoldButton: return {"control", "state", "v_in", "v_out"};
        case ComponentKind::IndicatorLight: return {"brightness", "v_in", "v_out"};
        case ComponentKind::InertiaNode: return {"damping", "inv_inertia", "mass", "rpm_out", "torque_in"};
        case ComponentKind::Integrator: return {"gain", "in", "out", "reset"};
        case ComponentKind::Inverter: return {"ac_out", "dc_in"};
        case ComponentKind::KnobSwitch: return {"control", "position", "throw1", "throw2", "throw3", "throw4", "throw5", "wiper"};
        case ComponentKind::LUT: return {"input", "output"};
        case ComponentKind::LerpNode: return {"input", "output"};
        case ComponentKind::Lesser: return {"A", "B", "o"};
        case ComponentKind::LesserEq: return {"A", "B", "o"};
        case ComponentKind::Max: return {"A", "B", "o"};
        case ComponentKind::Merger: return {"i1", "i2", "o"};
        case ComponentKind::Min: return {"A", "B", "o"};
        case ComponentKind::Monostable: return {"in", "out"};
        case ComponentKind::Multiply: return {"A", "B", "o"};
        case ComponentKind::NAND: return {"A", "B", "o"};
        case ComponentKind::NOT: return {"A", "o"};
        case ComponentKind::Normalize: return {"in", "max", "min", "out"};
        case ComponentKind::OR: return {"A", "B", "o"};
        case ComponentKind::P: return {"feedback", "output", "setpoint"};
        case ComponentKind::PD: return {"feedback", "output", "setpoint"};
        case ComponentKind::PI: return {"Ki", "Kp", "feedback", "output", "output_max", "output_min", "setpoint"};
        case ComponentKind::PID: return {"feedback", "output", "setpoint"};
        case ComponentKind::Positive_V_to_Bool: return {"Vin", "o"};
        case ComponentKind::Radiator: return {"heat_in", "heat_out"};
        case ComponentKind::RefNode: return {"v"};
        case ComponentKind::Relay: return {"control", "hold_threshold", "state", "v_in", "v_out"};
        case ComponentKind::Resistor: return {"v_in", "v_out"};
        case ComponentKind::RotarySwitch1ToN: return {"control", "position", "throw1", "throw2", "throw3", "throw4", "throw5", "wiper"};
        case ComponentKind::RotarySwitchNTo1: return {"control", "position", "throw1", "throw2", "throw3", "throw4", "throw5", "wiper"};
        case ComponentKind::SampleHold: return {"in", "out", "trigger"};
        case ComponentKind::SlewRate: return {"in", "out"};
        case ComponentKind::Slider: return {"control", "out"};
        case ComponentKind::SolenoidValve: return {"ctrl", "flow_in", "flow_out"};
        case ComponentKind::Splitter: return {"i", "o1", "o2"};
        case ComponentKind::Spring: return {"force_out", "pos_a", "pos_b"};
        case ComponentKind::Subtract: return {"A", "B", "o"};
        case ComponentKind::Switch: return {"control", "state", "v_in", "v_out"};
        case ComponentKind::TempSensor: return {"temp_in", "temp_out"};
        case ComponentKind::TimeDelay: return {"in", "out"};
        case ComponentKind::Transformer: return {"primary", "secondary"};
        case ComponentKind::Value: return {"o"};
        case ComponentKind::VariableConductance: return {"cmd", "g_max", "g_min", "v_in", "v_out"};
        case ComponentKind::VoltageSense: return {"gain", "offset", "out", "v_in", "v_ref"};
        case ComponentKind::Voltmeter: return {"v_in"};
        case ComponentKind::XOR: return {"A", "B", "o"};
        default: return {};
    }
}

inline bool has_component_metadata(ComponentKind kind) {
    return kind != ComponentKind::_COUNT;
}

inline std::vector<std::string> get_output_ports(ComponentKind kind) {
    std::vector<std::string> result;
    switch (kind) {
        case ComponentKind::AND:
            for (size_t i = 0; i < AND_PORT_COUNT; ++i) {
                if (AND_PORT_DIRECTIONS[i] == bp2::Direction::Output || AND_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(AND_PORTS[i]);
            }
            return result;
        case ComponentKind::AZS:
            for (size_t i = 0; i < AZS_PORT_COUNT; ++i) {
                if (AZS_PORT_DIRECTIONS[i] == bp2::Direction::Output || AZS_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(AZS_PORTS[i]);
            }
            return result;
        case ComponentKind::Accumulator:
            for (size_t i = 0; i < Accumulator_PORT_COUNT; ++i) {
                if (Accumulator_PORT_DIRECTIONS[i] == bp2::Direction::Output || Accumulator_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Accumulator_PORTS[i]);
            }
            return result;
        case ComponentKind::Add:
            for (size_t i = 0; i < Add_PORT_COUNT; ++i) {
                if (Add_PORT_DIRECTIONS[i] == bp2::Direction::Output || Add_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Add_PORTS[i]);
            }
            return result;
        case ComponentKind::Any_V_to_Bool:
            for (size_t i = 0; i < Any_V_to_Bool_PORT_COUNT; ++i) {
                if (Any_V_to_Bool_PORT_DIRECTIONS[i] == bp2::Direction::Output || Any_V_to_Bool_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Any_V_to_Bool_PORTS[i]);
            }
            return result;
        case ComponentKind::AsymSlewRate:
            for (size_t i = 0; i < AsymSlewRate_PORT_COUNT; ++i) {
                if (AsymSlewRate_PORT_DIRECTIONS[i] == bp2::Direction::Output || AsymSlewRate_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(AsymSlewRate_PORTS[i]);
            }
            return result;
        case ComponentKind::AsymTMO:
            for (size_t i = 0; i < AsymTMO_PORT_COUNT; ++i) {
                if (AsymTMO_PORT_DIRECTIONS[i] == bp2::Direction::Output || AsymTMO_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(AsymTMO_PORTS[i]);
            }
            return result;
        case ComponentKind::Bus:
            for (size_t i = 0; i < Bus_PORT_COUNT; ++i) {
                if (Bus_PORT_DIRECTIONS[i] == bp2::Direction::Output || Bus_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Bus_PORTS[i]);
            }
            return result;
        case ComponentKind::Clamp:
            for (size_t i = 0; i < Clamp_PORT_COUNT; ++i) {
                if (Clamp_PORT_DIRECTIONS[i] == bp2::Direction::Output || Clamp_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Clamp_PORTS[i]);
            }
            return result;
        case ComponentKind::Comparator:
            for (size_t i = 0; i < Comparator_PORT_COUNT; ++i) {
                if (Comparator_PORT_DIRECTIONS[i] == bp2::Direction::Output || Comparator_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Comparator_PORTS[i]);
            }
            return result;
        case ComponentKind::ControlledCurrentSource:
            for (size_t i = 0; i < ControlledCurrentSource_PORT_COUNT; ++i) {
                if (ControlledCurrentSource_PORT_DIRECTIONS[i] == bp2::Direction::Output || ControlledCurrentSource_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(ControlledCurrentSource_PORTS[i]);
            }
            return result;
        case ComponentKind::ControlledVoltageSource:
            for (size_t i = 0; i < ControlledVoltageSource_PORT_COUNT; ++i) {
                if (ControlledVoltageSource_PORT_DIRECTIONS[i] == bp2::Direction::Output || ControlledVoltageSource_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(ControlledVoltageSource_PORTS[i]);
            }
            return result;
        case ComponentKind::CurrentSense:
            for (size_t i = 0; i < CurrentSense_PORT_COUNT; ++i) {
                if (CurrentSense_PORT_DIRECTIONS[i] == bp2::Direction::Output || CurrentSense_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(CurrentSense_PORTS[i]);
            }
            return result;
        case ComponentKind::Divide:
            for (size_t i = 0; i < Divide_PORT_COUNT; ++i) {
                if (Divide_PORT_DIRECTIONS[i] == bp2::Direction::Output || Divide_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Divide_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricHeater:
            for (size_t i = 0; i < ElectricHeater_PORT_COUNT; ++i) {
                if (ElectricHeater_PORT_DIRECTIONS[i] == bp2::Direction::Output || ElectricHeater_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(ElectricHeater_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricPump:
            for (size_t i = 0; i < ElectricPump_PORT_COUNT; ++i) {
                if (ElectricPump_PORT_DIRECTIONS[i] == bp2::Direction::Output || ElectricPump_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(ElectricPump_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricalConductance:
            for (size_t i = 0; i < ElectricalConductance_PORT_COUNT; ++i) {
                if (ElectricalConductance_PORT_DIRECTIONS[i] == bp2::Direction::Output || ElectricalConductance_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(ElectricalConductance_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricalSource:
            for (size_t i = 0; i < ElectricalSource_PORT_COUNT; ++i) {
                if (ElectricalSource_PORT_DIRECTIONS[i] == bp2::Direction::Output || ElectricalSource_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(ElectricalSource_PORTS[i]);
            }
            return result;
        case ComponentKind::FastTMO:
            for (size_t i = 0; i < FastTMO_PORT_COUNT; ++i) {
                if (FastTMO_PORT_DIRECTIONS[i] == bp2::Direction::Output || FastTMO_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(FastTMO_PORTS[i]);
            }
            return result;
        case ComponentKind::FuelTank:
            for (size_t i = 0; i < FuelTank_PORT_COUNT; ++i) {
                if (FuelTank_PORT_DIRECTIONS[i] == bp2::Direction::Output || FuelTank_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(FuelTank_PORTS[i]);
            }
            return result;
        case ComponentKind::Generator:
            for (size_t i = 0; i < Generator_PORT_COUNT; ++i) {
                if (Generator_PORT_DIRECTIONS[i] == bp2::Direction::Output || Generator_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Generator_PORTS[i]);
            }
            return result;
        case ComponentKind::GidroAccumulator:
            for (size_t i = 0; i < GidroAccumulator_PORT_COUNT; ++i) {
                if (GidroAccumulator_PORT_DIRECTIONS[i] == bp2::Direction::Output || GidroAccumulator_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(GidroAccumulator_PORTS[i]);
            }
            return result;
        case ComponentKind::Greater:
            for (size_t i = 0; i < Greater_PORT_COUNT; ++i) {
                if (Greater_PORT_DIRECTIONS[i] == bp2::Direction::Output || Greater_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Greater_PORTS[i]);
            }
            return result;
        case ComponentKind::GreaterEq:
            for (size_t i = 0; i < GreaterEq_PORT_COUNT; ++i) {
                if (GreaterEq_PORT_DIRECTIONS[i] == bp2::Direction::Output || GreaterEq_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(GreaterEq_PORTS[i]);
            }
            return result;
        case ComponentKind::Gyroscope:
            for (size_t i = 0; i < Gyroscope_PORT_COUNT; ++i) {
                if (Gyroscope_PORT_DIRECTIONS[i] == bp2::Direction::Output || Gyroscope_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Gyroscope_PORTS[i]);
            }
            return result;
        case ComponentKind::HoldButton:
            for (size_t i = 0; i < HoldButton_PORT_COUNT; ++i) {
                if (HoldButton_PORT_DIRECTIONS[i] == bp2::Direction::Output || HoldButton_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(HoldButton_PORTS[i]);
            }
            return result;
        case ComponentKind::IndicatorLight:
            for (size_t i = 0; i < IndicatorLight_PORT_COUNT; ++i) {
                if (IndicatorLight_PORT_DIRECTIONS[i] == bp2::Direction::Output || IndicatorLight_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(IndicatorLight_PORTS[i]);
            }
            return result;
        case ComponentKind::InertiaNode:
            for (size_t i = 0; i < InertiaNode_PORT_COUNT; ++i) {
                if (InertiaNode_PORT_DIRECTIONS[i] == bp2::Direction::Output || InertiaNode_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(InertiaNode_PORTS[i]);
            }
            return result;
        case ComponentKind::Integrator:
            for (size_t i = 0; i < Integrator_PORT_COUNT; ++i) {
                if (Integrator_PORT_DIRECTIONS[i] == bp2::Direction::Output || Integrator_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Integrator_PORTS[i]);
            }
            return result;
        case ComponentKind::Inverter:
            for (size_t i = 0; i < Inverter_PORT_COUNT; ++i) {
                if (Inverter_PORT_DIRECTIONS[i] == bp2::Direction::Output || Inverter_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Inverter_PORTS[i]);
            }
            return result;
        case ComponentKind::KnobSwitch:
            for (size_t i = 0; i < KnobSwitch_PORT_COUNT; ++i) {
                if (KnobSwitch_PORT_DIRECTIONS[i] == bp2::Direction::Output || KnobSwitch_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(KnobSwitch_PORTS[i]);
            }
            return result;
        case ComponentKind::LUT:
            for (size_t i = 0; i < LUT_PORT_COUNT; ++i) {
                if (LUT_PORT_DIRECTIONS[i] == bp2::Direction::Output || LUT_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(LUT_PORTS[i]);
            }
            return result;
        case ComponentKind::LerpNode:
            for (size_t i = 0; i < LerpNode_PORT_COUNT; ++i) {
                if (LerpNode_PORT_DIRECTIONS[i] == bp2::Direction::Output || LerpNode_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(LerpNode_PORTS[i]);
            }
            return result;
        case ComponentKind::Lesser:
            for (size_t i = 0; i < Lesser_PORT_COUNT; ++i) {
                if (Lesser_PORT_DIRECTIONS[i] == bp2::Direction::Output || Lesser_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Lesser_PORTS[i]);
            }
            return result;
        case ComponentKind::LesserEq:
            for (size_t i = 0; i < LesserEq_PORT_COUNT; ++i) {
                if (LesserEq_PORT_DIRECTIONS[i] == bp2::Direction::Output || LesserEq_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(LesserEq_PORTS[i]);
            }
            return result;
        case ComponentKind::Max:
            for (size_t i = 0; i < Max_PORT_COUNT; ++i) {
                if (Max_PORT_DIRECTIONS[i] == bp2::Direction::Output || Max_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Max_PORTS[i]);
            }
            return result;
        case ComponentKind::Merger:
            for (size_t i = 0; i < Merger_PORT_COUNT; ++i) {
                if (Merger_PORT_DIRECTIONS[i] == bp2::Direction::Output || Merger_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Merger_PORTS[i]);
            }
            return result;
        case ComponentKind::Min:
            for (size_t i = 0; i < Min_PORT_COUNT; ++i) {
                if (Min_PORT_DIRECTIONS[i] == bp2::Direction::Output || Min_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Min_PORTS[i]);
            }
            return result;
        case ComponentKind::Monostable:
            for (size_t i = 0; i < Monostable_PORT_COUNT; ++i) {
                if (Monostable_PORT_DIRECTIONS[i] == bp2::Direction::Output || Monostable_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Monostable_PORTS[i]);
            }
            return result;
        case ComponentKind::Multiply:
            for (size_t i = 0; i < Multiply_PORT_COUNT; ++i) {
                if (Multiply_PORT_DIRECTIONS[i] == bp2::Direction::Output || Multiply_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Multiply_PORTS[i]);
            }
            return result;
        case ComponentKind::NAND:
            for (size_t i = 0; i < NAND_PORT_COUNT; ++i) {
                if (NAND_PORT_DIRECTIONS[i] == bp2::Direction::Output || NAND_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(NAND_PORTS[i]);
            }
            return result;
        case ComponentKind::NOT:
            for (size_t i = 0; i < NOT_PORT_COUNT; ++i) {
                if (NOT_PORT_DIRECTIONS[i] == bp2::Direction::Output || NOT_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(NOT_PORTS[i]);
            }
            return result;
        case ComponentKind::Normalize:
            for (size_t i = 0; i < Normalize_PORT_COUNT; ++i) {
                if (Normalize_PORT_DIRECTIONS[i] == bp2::Direction::Output || Normalize_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Normalize_PORTS[i]);
            }
            return result;
        case ComponentKind::OR:
            for (size_t i = 0; i < OR_PORT_COUNT; ++i) {
                if (OR_PORT_DIRECTIONS[i] == bp2::Direction::Output || OR_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(OR_PORTS[i]);
            }
            return result;
        case ComponentKind::P:
            for (size_t i = 0; i < P_PORT_COUNT; ++i) {
                if (P_PORT_DIRECTIONS[i] == bp2::Direction::Output || P_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(P_PORTS[i]);
            }
            return result;
        case ComponentKind::PD:
            for (size_t i = 0; i < PD_PORT_COUNT; ++i) {
                if (PD_PORT_DIRECTIONS[i] == bp2::Direction::Output || PD_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(PD_PORTS[i]);
            }
            return result;
        case ComponentKind::PI:
            for (size_t i = 0; i < PI_PORT_COUNT; ++i) {
                if (PI_PORT_DIRECTIONS[i] == bp2::Direction::Output || PI_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(PI_PORTS[i]);
            }
            return result;
        case ComponentKind::PID:
            for (size_t i = 0; i < PID_PORT_COUNT; ++i) {
                if (PID_PORT_DIRECTIONS[i] == bp2::Direction::Output || PID_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(PID_PORTS[i]);
            }
            return result;
        case ComponentKind::Positive_V_to_Bool:
            for (size_t i = 0; i < Positive_V_to_Bool_PORT_COUNT; ++i) {
                if (Positive_V_to_Bool_PORT_DIRECTIONS[i] == bp2::Direction::Output || Positive_V_to_Bool_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Positive_V_to_Bool_PORTS[i]);
            }
            return result;
        case ComponentKind::Radiator:
            for (size_t i = 0; i < Radiator_PORT_COUNT; ++i) {
                if (Radiator_PORT_DIRECTIONS[i] == bp2::Direction::Output || Radiator_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Radiator_PORTS[i]);
            }
            return result;
        case ComponentKind::RefNode:
            for (size_t i = 0; i < RefNode_PORT_COUNT; ++i) {
                if (RefNode_PORT_DIRECTIONS[i] == bp2::Direction::Output || RefNode_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(RefNode_PORTS[i]);
            }
            return result;
        case ComponentKind::Relay:
            for (size_t i = 0; i < Relay_PORT_COUNT; ++i) {
                if (Relay_PORT_DIRECTIONS[i] == bp2::Direction::Output || Relay_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Relay_PORTS[i]);
            }
            return result;
        case ComponentKind::Resistor:
            for (size_t i = 0; i < Resistor_PORT_COUNT; ++i) {
                if (Resistor_PORT_DIRECTIONS[i] == bp2::Direction::Output || Resistor_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Resistor_PORTS[i]);
            }
            return result;
        case ComponentKind::RotarySwitch1ToN:
            for (size_t i = 0; i < RotarySwitch1ToN_PORT_COUNT; ++i) {
                if (RotarySwitch1ToN_PORT_DIRECTIONS[i] == bp2::Direction::Output || RotarySwitch1ToN_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(RotarySwitch1ToN_PORTS[i]);
            }
            return result;
        case ComponentKind::RotarySwitchNTo1:
            for (size_t i = 0; i < RotarySwitchNTo1_PORT_COUNT; ++i) {
                if (RotarySwitchNTo1_PORT_DIRECTIONS[i] == bp2::Direction::Output || RotarySwitchNTo1_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(RotarySwitchNTo1_PORTS[i]);
            }
            return result;
        case ComponentKind::SampleHold:
            for (size_t i = 0; i < SampleHold_PORT_COUNT; ++i) {
                if (SampleHold_PORT_DIRECTIONS[i] == bp2::Direction::Output || SampleHold_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(SampleHold_PORTS[i]);
            }
            return result;
        case ComponentKind::SlewRate:
            for (size_t i = 0; i < SlewRate_PORT_COUNT; ++i) {
                if (SlewRate_PORT_DIRECTIONS[i] == bp2::Direction::Output || SlewRate_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(SlewRate_PORTS[i]);
            }
            return result;
        case ComponentKind::Slider:
            for (size_t i = 0; i < Slider_PORT_COUNT; ++i) {
                if (Slider_PORT_DIRECTIONS[i] == bp2::Direction::Output || Slider_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Slider_PORTS[i]);
            }
            return result;
        case ComponentKind::SolenoidValve:
            for (size_t i = 0; i < SolenoidValve_PORT_COUNT; ++i) {
                if (SolenoidValve_PORT_DIRECTIONS[i] == bp2::Direction::Output || SolenoidValve_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(SolenoidValve_PORTS[i]);
            }
            return result;
        case ComponentKind::Splitter:
            for (size_t i = 0; i < Splitter_PORT_COUNT; ++i) {
                if (Splitter_PORT_DIRECTIONS[i] == bp2::Direction::Output || Splitter_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Splitter_PORTS[i]);
            }
            return result;
        case ComponentKind::Spring:
            for (size_t i = 0; i < Spring_PORT_COUNT; ++i) {
                if (Spring_PORT_DIRECTIONS[i] == bp2::Direction::Output || Spring_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Spring_PORTS[i]);
            }
            return result;
        case ComponentKind::Subtract:
            for (size_t i = 0; i < Subtract_PORT_COUNT; ++i) {
                if (Subtract_PORT_DIRECTIONS[i] == bp2::Direction::Output || Subtract_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Subtract_PORTS[i]);
            }
            return result;
        case ComponentKind::Switch:
            for (size_t i = 0; i < Switch_PORT_COUNT; ++i) {
                if (Switch_PORT_DIRECTIONS[i] == bp2::Direction::Output || Switch_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Switch_PORTS[i]);
            }
            return result;
        case ComponentKind::TempSensor:
            for (size_t i = 0; i < TempSensor_PORT_COUNT; ++i) {
                if (TempSensor_PORT_DIRECTIONS[i] == bp2::Direction::Output || TempSensor_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(TempSensor_PORTS[i]);
            }
            return result;
        case ComponentKind::TimeDelay:
            for (size_t i = 0; i < TimeDelay_PORT_COUNT; ++i) {
                if (TimeDelay_PORT_DIRECTIONS[i] == bp2::Direction::Output || TimeDelay_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(TimeDelay_PORTS[i]);
            }
            return result;
        case ComponentKind::Transformer:
            for (size_t i = 0; i < Transformer_PORT_COUNT; ++i) {
                if (Transformer_PORT_DIRECTIONS[i] == bp2::Direction::Output || Transformer_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Transformer_PORTS[i]);
            }
            return result;
        case ComponentKind::Value:
            for (size_t i = 0; i < Value_PORT_COUNT; ++i) {
                if (Value_PORT_DIRECTIONS[i] == bp2::Direction::Output || Value_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Value_PORTS[i]);
            }
            return result;
        case ComponentKind::VariableConductance:
            for (size_t i = 0; i < VariableConductance_PORT_COUNT; ++i) {
                if (VariableConductance_PORT_DIRECTIONS[i] == bp2::Direction::Output || VariableConductance_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(VariableConductance_PORTS[i]);
            }
            return result;
        case ComponentKind::VoltageSense:
            for (size_t i = 0; i < VoltageSense_PORT_COUNT; ++i) {
                if (VoltageSense_PORT_DIRECTIONS[i] == bp2::Direction::Output || VoltageSense_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(VoltageSense_PORTS[i]);
            }
            return result;
        case ComponentKind::Voltmeter:
            for (size_t i = 0; i < Voltmeter_PORT_COUNT; ++i) {
                if (Voltmeter_PORT_DIRECTIONS[i] == bp2::Direction::Output || Voltmeter_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(Voltmeter_PORTS[i]);
            }
            return result;
        case ComponentKind::XOR:
            for (size_t i = 0; i < XOR_PORT_COUNT; ++i) {
                if (XOR_PORT_DIRECTIONS[i] == bp2::Direction::Output || XOR_PORT_DIRECTIONS[i] == bp2::Direction::InOut)
                    result.push_back(XOR_PORTS[i]);
            }
            return result;
        default: return result;
    }
}

inline std::vector<std::string> get_source_writer_ports(ComponentKind kind, uint8_t domain_mask) {
    std::vector<std::string> result;
    switch (kind) {
        case ComponentKind::AND:
            for (size_t i = 0; i < AND_PORT_COUNT; ++i) {
                if (AND_PORT_SOURCE_WRITER[i] && ((AND_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(AND_PORTS[i]);
            }
            return result;
        case ComponentKind::AZS:
            for (size_t i = 0; i < AZS_PORT_COUNT; ++i) {
                if (AZS_PORT_SOURCE_WRITER[i] && ((AZS_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(AZS_PORTS[i]);
            }
            return result;
        case ComponentKind::Accumulator:
            for (size_t i = 0; i < Accumulator_PORT_COUNT; ++i) {
                if (Accumulator_PORT_SOURCE_WRITER[i] && ((Accumulator_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Accumulator_PORTS[i]);
            }
            return result;
        case ComponentKind::Add:
            for (size_t i = 0; i < Add_PORT_COUNT; ++i) {
                if (Add_PORT_SOURCE_WRITER[i] && ((Add_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Add_PORTS[i]);
            }
            return result;
        case ComponentKind::Any_V_to_Bool:
            for (size_t i = 0; i < Any_V_to_Bool_PORT_COUNT; ++i) {
                if (Any_V_to_Bool_PORT_SOURCE_WRITER[i] && ((Any_V_to_Bool_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Any_V_to_Bool_PORTS[i]);
            }
            return result;
        case ComponentKind::AsymSlewRate:
            for (size_t i = 0; i < AsymSlewRate_PORT_COUNT; ++i) {
                if (AsymSlewRate_PORT_SOURCE_WRITER[i] && ((AsymSlewRate_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(AsymSlewRate_PORTS[i]);
            }
            return result;
        case ComponentKind::AsymTMO:
            for (size_t i = 0; i < AsymTMO_PORT_COUNT; ++i) {
                if (AsymTMO_PORT_SOURCE_WRITER[i] && ((AsymTMO_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(AsymTMO_PORTS[i]);
            }
            return result;
        case ComponentKind::Bus:
            for (size_t i = 0; i < Bus_PORT_COUNT; ++i) {
                if (Bus_PORT_SOURCE_WRITER[i] && ((Bus_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Bus_PORTS[i]);
            }
            return result;
        case ComponentKind::Clamp:
            for (size_t i = 0; i < Clamp_PORT_COUNT; ++i) {
                if (Clamp_PORT_SOURCE_WRITER[i] && ((Clamp_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Clamp_PORTS[i]);
            }
            return result;
        case ComponentKind::Comparator:
            for (size_t i = 0; i < Comparator_PORT_COUNT; ++i) {
                if (Comparator_PORT_SOURCE_WRITER[i] && ((Comparator_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Comparator_PORTS[i]);
            }
            return result;
        case ComponentKind::ControlledCurrentSource:
            for (size_t i = 0; i < ControlledCurrentSource_PORT_COUNT; ++i) {
                if (ControlledCurrentSource_PORT_SOURCE_WRITER[i] && ((ControlledCurrentSource_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(ControlledCurrentSource_PORTS[i]);
            }
            return result;
        case ComponentKind::ControlledVoltageSource:
            for (size_t i = 0; i < ControlledVoltageSource_PORT_COUNT; ++i) {
                if (ControlledVoltageSource_PORT_SOURCE_WRITER[i] && ((ControlledVoltageSource_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(ControlledVoltageSource_PORTS[i]);
            }
            return result;
        case ComponentKind::CurrentSense:
            for (size_t i = 0; i < CurrentSense_PORT_COUNT; ++i) {
                if (CurrentSense_PORT_SOURCE_WRITER[i] && ((CurrentSense_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(CurrentSense_PORTS[i]);
            }
            return result;
        case ComponentKind::Divide:
            for (size_t i = 0; i < Divide_PORT_COUNT; ++i) {
                if (Divide_PORT_SOURCE_WRITER[i] && ((Divide_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Divide_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricHeater:
            for (size_t i = 0; i < ElectricHeater_PORT_COUNT; ++i) {
                if (ElectricHeater_PORT_SOURCE_WRITER[i] && ((ElectricHeater_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(ElectricHeater_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricPump:
            for (size_t i = 0; i < ElectricPump_PORT_COUNT; ++i) {
                if (ElectricPump_PORT_SOURCE_WRITER[i] && ((ElectricPump_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(ElectricPump_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricalConductance:
            for (size_t i = 0; i < ElectricalConductance_PORT_COUNT; ++i) {
                if (ElectricalConductance_PORT_SOURCE_WRITER[i] && ((ElectricalConductance_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(ElectricalConductance_PORTS[i]);
            }
            return result;
        case ComponentKind::ElectricalSource:
            for (size_t i = 0; i < ElectricalSource_PORT_COUNT; ++i) {
                if (ElectricalSource_PORT_SOURCE_WRITER[i] && ((ElectricalSource_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(ElectricalSource_PORTS[i]);
            }
            return result;
        case ComponentKind::FastTMO:
            for (size_t i = 0; i < FastTMO_PORT_COUNT; ++i) {
                if (FastTMO_PORT_SOURCE_WRITER[i] && ((FastTMO_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(FastTMO_PORTS[i]);
            }
            return result;
        case ComponentKind::FuelTank:
            for (size_t i = 0; i < FuelTank_PORT_COUNT; ++i) {
                if (FuelTank_PORT_SOURCE_WRITER[i] && ((FuelTank_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(FuelTank_PORTS[i]);
            }
            return result;
        case ComponentKind::Generator:
            for (size_t i = 0; i < Generator_PORT_COUNT; ++i) {
                if (Generator_PORT_SOURCE_WRITER[i] && ((Generator_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Generator_PORTS[i]);
            }
            return result;
        case ComponentKind::GidroAccumulator:
            for (size_t i = 0; i < GidroAccumulator_PORT_COUNT; ++i) {
                if (GidroAccumulator_PORT_SOURCE_WRITER[i] && ((GidroAccumulator_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(GidroAccumulator_PORTS[i]);
            }
            return result;
        case ComponentKind::Greater:
            for (size_t i = 0; i < Greater_PORT_COUNT; ++i) {
                if (Greater_PORT_SOURCE_WRITER[i] && ((Greater_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Greater_PORTS[i]);
            }
            return result;
        case ComponentKind::GreaterEq:
            for (size_t i = 0; i < GreaterEq_PORT_COUNT; ++i) {
                if (GreaterEq_PORT_SOURCE_WRITER[i] && ((GreaterEq_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(GreaterEq_PORTS[i]);
            }
            return result;
        case ComponentKind::Gyroscope:
            for (size_t i = 0; i < Gyroscope_PORT_COUNT; ++i) {
                if (Gyroscope_PORT_SOURCE_WRITER[i] && ((Gyroscope_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Gyroscope_PORTS[i]);
            }
            return result;
        case ComponentKind::HoldButton:
            for (size_t i = 0; i < HoldButton_PORT_COUNT; ++i) {
                if (HoldButton_PORT_SOURCE_WRITER[i] && ((HoldButton_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(HoldButton_PORTS[i]);
            }
            return result;
        case ComponentKind::IndicatorLight:
            for (size_t i = 0; i < IndicatorLight_PORT_COUNT; ++i) {
                if (IndicatorLight_PORT_SOURCE_WRITER[i] && ((IndicatorLight_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(IndicatorLight_PORTS[i]);
            }
            return result;
        case ComponentKind::InertiaNode:
            for (size_t i = 0; i < InertiaNode_PORT_COUNT; ++i) {
                if (InertiaNode_PORT_SOURCE_WRITER[i] && ((InertiaNode_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(InertiaNode_PORTS[i]);
            }
            return result;
        case ComponentKind::Integrator:
            for (size_t i = 0; i < Integrator_PORT_COUNT; ++i) {
                if (Integrator_PORT_SOURCE_WRITER[i] && ((Integrator_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Integrator_PORTS[i]);
            }
            return result;
        case ComponentKind::Inverter:
            for (size_t i = 0; i < Inverter_PORT_COUNT; ++i) {
                if (Inverter_PORT_SOURCE_WRITER[i] && ((Inverter_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Inverter_PORTS[i]);
            }
            return result;
        case ComponentKind::KnobSwitch:
            for (size_t i = 0; i < KnobSwitch_PORT_COUNT; ++i) {
                if (KnobSwitch_PORT_SOURCE_WRITER[i] && ((KnobSwitch_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(KnobSwitch_PORTS[i]);
            }
            return result;
        case ComponentKind::LUT:
            for (size_t i = 0; i < LUT_PORT_COUNT; ++i) {
                if (LUT_PORT_SOURCE_WRITER[i] && ((LUT_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(LUT_PORTS[i]);
            }
            return result;
        case ComponentKind::LerpNode:
            for (size_t i = 0; i < LerpNode_PORT_COUNT; ++i) {
                if (LerpNode_PORT_SOURCE_WRITER[i] && ((LerpNode_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(LerpNode_PORTS[i]);
            }
            return result;
        case ComponentKind::Lesser:
            for (size_t i = 0; i < Lesser_PORT_COUNT; ++i) {
                if (Lesser_PORT_SOURCE_WRITER[i] && ((Lesser_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Lesser_PORTS[i]);
            }
            return result;
        case ComponentKind::LesserEq:
            for (size_t i = 0; i < LesserEq_PORT_COUNT; ++i) {
                if (LesserEq_PORT_SOURCE_WRITER[i] && ((LesserEq_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(LesserEq_PORTS[i]);
            }
            return result;
        case ComponentKind::Max:
            for (size_t i = 0; i < Max_PORT_COUNT; ++i) {
                if (Max_PORT_SOURCE_WRITER[i] && ((Max_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Max_PORTS[i]);
            }
            return result;
        case ComponentKind::Merger:
            for (size_t i = 0; i < Merger_PORT_COUNT; ++i) {
                if (Merger_PORT_SOURCE_WRITER[i] && ((Merger_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Merger_PORTS[i]);
            }
            return result;
        case ComponentKind::Min:
            for (size_t i = 0; i < Min_PORT_COUNT; ++i) {
                if (Min_PORT_SOURCE_WRITER[i] && ((Min_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Min_PORTS[i]);
            }
            return result;
        case ComponentKind::Monostable:
            for (size_t i = 0; i < Monostable_PORT_COUNT; ++i) {
                if (Monostable_PORT_SOURCE_WRITER[i] && ((Monostable_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Monostable_PORTS[i]);
            }
            return result;
        case ComponentKind::Multiply:
            for (size_t i = 0; i < Multiply_PORT_COUNT; ++i) {
                if (Multiply_PORT_SOURCE_WRITER[i] && ((Multiply_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Multiply_PORTS[i]);
            }
            return result;
        case ComponentKind::NAND:
            for (size_t i = 0; i < NAND_PORT_COUNT; ++i) {
                if (NAND_PORT_SOURCE_WRITER[i] && ((NAND_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(NAND_PORTS[i]);
            }
            return result;
        case ComponentKind::NOT:
            for (size_t i = 0; i < NOT_PORT_COUNT; ++i) {
                if (NOT_PORT_SOURCE_WRITER[i] && ((NOT_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(NOT_PORTS[i]);
            }
            return result;
        case ComponentKind::Normalize:
            for (size_t i = 0; i < Normalize_PORT_COUNT; ++i) {
                if (Normalize_PORT_SOURCE_WRITER[i] && ((Normalize_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Normalize_PORTS[i]);
            }
            return result;
        case ComponentKind::OR:
            for (size_t i = 0; i < OR_PORT_COUNT; ++i) {
                if (OR_PORT_SOURCE_WRITER[i] && ((OR_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(OR_PORTS[i]);
            }
            return result;
        case ComponentKind::P:
            for (size_t i = 0; i < P_PORT_COUNT; ++i) {
                if (P_PORT_SOURCE_WRITER[i] && ((P_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(P_PORTS[i]);
            }
            return result;
        case ComponentKind::PD:
            for (size_t i = 0; i < PD_PORT_COUNT; ++i) {
                if (PD_PORT_SOURCE_WRITER[i] && ((PD_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(PD_PORTS[i]);
            }
            return result;
        case ComponentKind::PI:
            for (size_t i = 0; i < PI_PORT_COUNT; ++i) {
                if (PI_PORT_SOURCE_WRITER[i] && ((PI_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(PI_PORTS[i]);
            }
            return result;
        case ComponentKind::PID:
            for (size_t i = 0; i < PID_PORT_COUNT; ++i) {
                if (PID_PORT_SOURCE_WRITER[i] && ((PID_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(PID_PORTS[i]);
            }
            return result;
        case ComponentKind::Positive_V_to_Bool:
            for (size_t i = 0; i < Positive_V_to_Bool_PORT_COUNT; ++i) {
                if (Positive_V_to_Bool_PORT_SOURCE_WRITER[i] && ((Positive_V_to_Bool_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Positive_V_to_Bool_PORTS[i]);
            }
            return result;
        case ComponentKind::Radiator:
            for (size_t i = 0; i < Radiator_PORT_COUNT; ++i) {
                if (Radiator_PORT_SOURCE_WRITER[i] && ((Radiator_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Radiator_PORTS[i]);
            }
            return result;
        case ComponentKind::RefNode:
            for (size_t i = 0; i < RefNode_PORT_COUNT; ++i) {
                if (RefNode_PORT_SOURCE_WRITER[i] && ((RefNode_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(RefNode_PORTS[i]);
            }
            return result;
        case ComponentKind::Relay:
            for (size_t i = 0; i < Relay_PORT_COUNT; ++i) {
                if (Relay_PORT_SOURCE_WRITER[i] && ((Relay_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Relay_PORTS[i]);
            }
            return result;
        case ComponentKind::Resistor:
            for (size_t i = 0; i < Resistor_PORT_COUNT; ++i) {
                if (Resistor_PORT_SOURCE_WRITER[i] && ((Resistor_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Resistor_PORTS[i]);
            }
            return result;
        case ComponentKind::RotarySwitch1ToN:
            for (size_t i = 0; i < RotarySwitch1ToN_PORT_COUNT; ++i) {
                if (RotarySwitch1ToN_PORT_SOURCE_WRITER[i] && ((RotarySwitch1ToN_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(RotarySwitch1ToN_PORTS[i]);
            }
            return result;
        case ComponentKind::RotarySwitchNTo1:
            for (size_t i = 0; i < RotarySwitchNTo1_PORT_COUNT; ++i) {
                if (RotarySwitchNTo1_PORT_SOURCE_WRITER[i] && ((RotarySwitchNTo1_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(RotarySwitchNTo1_PORTS[i]);
            }
            return result;
        case ComponentKind::SampleHold:
            for (size_t i = 0; i < SampleHold_PORT_COUNT; ++i) {
                if (SampleHold_PORT_SOURCE_WRITER[i] && ((SampleHold_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(SampleHold_PORTS[i]);
            }
            return result;
        case ComponentKind::SlewRate:
            for (size_t i = 0; i < SlewRate_PORT_COUNT; ++i) {
                if (SlewRate_PORT_SOURCE_WRITER[i] && ((SlewRate_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(SlewRate_PORTS[i]);
            }
            return result;
        case ComponentKind::Slider:
            for (size_t i = 0; i < Slider_PORT_COUNT; ++i) {
                if (Slider_PORT_SOURCE_WRITER[i] && ((Slider_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Slider_PORTS[i]);
            }
            return result;
        case ComponentKind::SolenoidValve:
            for (size_t i = 0; i < SolenoidValve_PORT_COUNT; ++i) {
                if (SolenoidValve_PORT_SOURCE_WRITER[i] && ((SolenoidValve_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(SolenoidValve_PORTS[i]);
            }
            return result;
        case ComponentKind::Splitter:
            for (size_t i = 0; i < Splitter_PORT_COUNT; ++i) {
                if (Splitter_PORT_SOURCE_WRITER[i] && ((Splitter_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Splitter_PORTS[i]);
            }
            return result;
        case ComponentKind::Spring:
            for (size_t i = 0; i < Spring_PORT_COUNT; ++i) {
                if (Spring_PORT_SOURCE_WRITER[i] && ((Spring_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Spring_PORTS[i]);
            }
            return result;
        case ComponentKind::Subtract:
            for (size_t i = 0; i < Subtract_PORT_COUNT; ++i) {
                if (Subtract_PORT_SOURCE_WRITER[i] && ((Subtract_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Subtract_PORTS[i]);
            }
            return result;
        case ComponentKind::Switch:
            for (size_t i = 0; i < Switch_PORT_COUNT; ++i) {
                if (Switch_PORT_SOURCE_WRITER[i] && ((Switch_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Switch_PORTS[i]);
            }
            return result;
        case ComponentKind::TempSensor:
            for (size_t i = 0; i < TempSensor_PORT_COUNT; ++i) {
                if (TempSensor_PORT_SOURCE_WRITER[i] && ((TempSensor_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(TempSensor_PORTS[i]);
            }
            return result;
        case ComponentKind::TimeDelay:
            for (size_t i = 0; i < TimeDelay_PORT_COUNT; ++i) {
                if (TimeDelay_PORT_SOURCE_WRITER[i] && ((TimeDelay_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(TimeDelay_PORTS[i]);
            }
            return result;
        case ComponentKind::Transformer:
            for (size_t i = 0; i < Transformer_PORT_COUNT; ++i) {
                if (Transformer_PORT_SOURCE_WRITER[i] && ((Transformer_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Transformer_PORTS[i]);
            }
            return result;
        case ComponentKind::Value:
            for (size_t i = 0; i < Value_PORT_COUNT; ++i) {
                if (Value_PORT_SOURCE_WRITER[i] && ((Value_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Value_PORTS[i]);
            }
            return result;
        case ComponentKind::VariableConductance:
            for (size_t i = 0; i < VariableConductance_PORT_COUNT; ++i) {
                if (VariableConductance_PORT_SOURCE_WRITER[i] && ((VariableConductance_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(VariableConductance_PORTS[i]);
            }
            return result;
        case ComponentKind::VoltageSense:
            for (size_t i = 0; i < VoltageSense_PORT_COUNT; ++i) {
                if (VoltageSense_PORT_SOURCE_WRITER[i] && ((VoltageSense_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(VoltageSense_PORTS[i]);
            }
            return result;
        case ComponentKind::Voltmeter:
            for (size_t i = 0; i < Voltmeter_PORT_COUNT; ++i) {
                if (Voltmeter_PORT_SOURCE_WRITER[i] && ((Voltmeter_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(Voltmeter_PORTS[i]);
            }
            return result;
        case ComponentKind::XOR:
            for (size_t i = 0; i < XOR_PORT_COUNT; ++i) {
                if (XOR_PORT_SOURCE_WRITER[i] && ((XOR_PORT_DOMAINS[i] & domain_mask) != 0))
                    result.push_back(XOR_PORTS[i]);
            }
            return result;
        default: return result;
    }
}

constexpr bool COMPONENT_SCHEDULER_SOURCE[] = {
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false
};

constexpr bool COMPONENT_SOLVER_OWNED_ELECTRICAL[] = {
    false,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    true,
    true,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    true,
    true,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false
};

constexpr bool COMPONENT_REQUIRES_SOLVER_ROLE[] = {
    false,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    true,
    false,
    false,
    false,
    true,
    true,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    true,
    true,
    false,
    false,
    false,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    true,
    true,
    true,
    true,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    false,
    true,
    false,
    false,
    false
};

inline bool is_scheduler_source_component(ComponentKind kind) {
    const auto idx = static_cast<size_t>(kind);
    return idx < static_cast<size_t>(ComponentKind::_COUNT) && COMPONENT_SCHEDULER_SOURCE[idx];
}

inline bool is_solver_owned_electrical_component(ComponentKind kind) {
    const auto idx = static_cast<size_t>(kind);
    return idx < static_cast<size_t>(ComponentKind::_COUNT) && COMPONENT_SOLVER_OWNED_ELECTRICAL[idx];
}

inline bool requires_solver_role_component(ComponentKind kind) {
    const auto idx = static_cast<size_t>(kind);
    return idx < static_cast<size_t>(ComponentKind::_COUNT) && COMPONENT_REQUIRES_SOLVER_ROLE[idx];
}

using ComponentVariant = std::variant<
    AND<JitProvider>,
    AZS<JitProvider>,
    Accumulator<JitProvider>,
    Add<JitProvider>,
    Any_V_to_Bool<JitProvider>,
    AsymSlewRate<JitProvider>,
    AsymTMO<JitProvider>,
    Bus<JitProvider>,
    Clamp<JitProvider>,
    Comparator<JitProvider>,
    ControlledCurrentSource<JitProvider>,
    ControlledVoltageSource<JitProvider>,
    CurrentSense<JitProvider>,
    Divide<JitProvider>,
    ElectricHeater<JitProvider>,
    ElectricPump<JitProvider>,
    ElectricalConductance<JitProvider>,
    ElectricalSource<JitProvider>,
    FastTMO<JitProvider>,
    FuelTank<JitProvider>,
    Generator<JitProvider>,
    GidroAccumulator<JitProvider>,
    Greater<JitProvider>,
    GreaterEq<JitProvider>,
    Gyroscope<JitProvider>,
    HoldButton<JitProvider>,
    IndicatorLight<JitProvider>,
    InertiaNode<JitProvider>,
    Integrator<JitProvider>,
    Inverter<JitProvider>,
    KnobSwitch<JitProvider>,
    LUT<JitProvider>,
    LerpNode<JitProvider>,
    Lesser<JitProvider>,
    LesserEq<JitProvider>,
    Max<JitProvider>,
    Merger<JitProvider>,
    Min<JitProvider>,
    Monostable<JitProvider>,
    Multiply<JitProvider>,
    NAND<JitProvider>,
    NOT<JitProvider>,
    Normalize<JitProvider>,
    OR<JitProvider>,
    P<JitProvider>,
    PD<JitProvider>,
    PI<JitProvider>,
    PID<JitProvider>,
    Positive_V_to_Bool<JitProvider>,
    Radiator<JitProvider>,
    RefNode<JitProvider>,
    Relay<JitProvider>,
    Resistor<JitProvider>,
    RotarySwitch1ToN<JitProvider>,
    RotarySwitchNTo1<JitProvider>,
    SampleHold<JitProvider>,
    SlewRate<JitProvider>,
    Slider<JitProvider>,
    SolenoidValve<JitProvider>,
    Splitter<JitProvider>,
    Spring<JitProvider>,
    Subtract<JitProvider>,
    Switch<JitProvider>,
    TempSensor<JitProvider>,
    TimeDelay<JitProvider>,
    Transformer<JitProvider>,
    Value<JitProvider>,
    VariableConductance<JitProvider>,
    VoltageSense<JitProvider>,
    Voltmeter<JitProvider>,
    XOR<JitProvider>
>;

static_assert(
    std::variant_size_v<ComponentVariant> == static_cast<size_t>(ComponentKind::_COUNT),
    "ComponentKind enum and ComponentVariant are out of sync — regenerate port_registry.h"
);

// Compile-time ordering check: enum ↔ classname round-trip
static_assert(static_cast<size_t>(ComponentKind::AND) == 0ull, "AND enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::AZS) == 1ull, "AZS enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Accumulator) == 2ull, "Accumulator enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Add) == 3ull, "Add enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Any_V_to_Bool) == 4ull, "Any_V_to_Bool enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::AsymSlewRate) == 5ull, "AsymSlewRate enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::AsymTMO) == 6ull, "AsymTMO enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Bus) == 7ull, "Bus enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Clamp) == 8ull, "Clamp enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Comparator) == 9ull, "Comparator enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::ControlledCurrentSource) == 10ull, "ControlledCurrentSource enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::ControlledVoltageSource) == 11ull, "ControlledVoltageSource enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::CurrentSense) == 12ull, "CurrentSense enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Divide) == 13ull, "Divide enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::ElectricHeater) == 14ull, "ElectricHeater enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::ElectricPump) == 15ull, "ElectricPump enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::ElectricalConductance) == 16ull, "ElectricalConductance enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::ElectricalSource) == 17ull, "ElectricalSource enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::FastTMO) == 18ull, "FastTMO enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::FuelTank) == 19ull, "FuelTank enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Generator) == 20ull, "Generator enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::GidroAccumulator) == 21ull, "GidroAccumulator enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Greater) == 22ull, "Greater enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::GreaterEq) == 23ull, "GreaterEq enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Gyroscope) == 24ull, "Gyroscope enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::HoldButton) == 25ull, "HoldButton enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::IndicatorLight) == 26ull, "IndicatorLight enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::InertiaNode) == 27ull, "InertiaNode enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Integrator) == 28ull, "Integrator enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Inverter) == 29ull, "Inverter enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::KnobSwitch) == 30ull, "KnobSwitch enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::LUT) == 31ull, "LUT enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::LerpNode) == 32ull, "LerpNode enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Lesser) == 33ull, "Lesser enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::LesserEq) == 34ull, "LesserEq enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Max) == 35ull, "Max enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Merger) == 36ull, "Merger enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Min) == 37ull, "Min enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Monostable) == 38ull, "Monostable enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Multiply) == 39ull, "Multiply enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::NAND) == 40ull, "NAND enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::NOT) == 41ull, "NOT enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Normalize) == 42ull, "Normalize enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::OR) == 43ull, "OR enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::P) == 44ull, "P enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::PD) == 45ull, "PD enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::PI) == 46ull, "PI enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::PID) == 47ull, "PID enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Positive_V_to_Bool) == 48ull, "Positive_V_to_Bool enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Radiator) == 49ull, "Radiator enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::RefNode) == 50ull, "RefNode enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Relay) == 51ull, "Relay enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Resistor) == 52ull, "Resistor enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::RotarySwitch1ToN) == 53ull, "RotarySwitch1ToN enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::RotarySwitchNTo1) == 54ull, "RotarySwitchNTo1 enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::SampleHold) == 55ull, "SampleHold enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::SlewRate) == 56ull, "SlewRate enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Slider) == 57ull, "Slider enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::SolenoidValve) == 58ull, "SolenoidValve enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Splitter) == 59ull, "Splitter enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Spring) == 60ull, "Spring enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Subtract) == 61ull, "Subtract enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Switch) == 62ull, "Switch enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::TempSensor) == 63ull, "TempSensor enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::TimeDelay) == 64ull, "TimeDelay enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Transformer) == 65ull, "Transformer enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Value) == 66ull, "Value enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::VariableConductance) == 67ull, "VariableConductance enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::VoltageSense) == 68ull, "VoltageSense enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::Voltmeter) == 69ull, "Voltmeter enum position mismatch — regenerate port_registry.h or update component_kind.h");
static_assert(static_cast<size_t>(ComponentKind::XOR) == 70ull, "XOR enum position mismatch — regenerate port_registry.h or update component_kind.h");

