#pragma once

/// NodeContentRenderer is now obsolete.
/// Content is rendered via the widget scene graph (NodeWidget children).
/// Interaction is handled via semantic scene snapshots and semantic controllers.
