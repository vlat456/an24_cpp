// NodeContentRenderer is now obsolete.
// All content rendering is done via widget scene graph (NodeWidget children).
// All interaction is done via semantic scene snapshots and semantic controllers.
